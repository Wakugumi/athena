export enum ListingEvents {
  PUBLISHED = 'listing.published',
  ITEM_ADDED = 'listing.item_added',
  ITEM_READY = 'listing.item_ready',
  UPDATED = 'listing.updated',
  REMOVED = 'listing.removed'
}
